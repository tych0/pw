language: rust
rust:
    - stable
    - beta
    - nightly
matrix:
    allow_failures:
        - rust: nightly
cache: cargo
before_script:
    - cargo install rustfmt
script:
    - rustfmt src/* && git diff --quiet
    - ./test.sh
